@page
@model WebApplication2.Pages.Measure.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TemperatureMeasurement[0].Temperature)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TemperatureMeasurement[0].ApplicationUserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TemperatureMeasurement[0].MeasureDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TemperatureMeasurement[0].OfficeToday)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TemperatureMeasurement[0].OfficeTomorrow)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TemperatureMeasurement) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Temperature)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ApplicationUserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MeasureDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OfficeToday)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OfficeTomorrow)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
